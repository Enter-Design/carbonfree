---
interface Props { class?: string }
const { class: className = '' } = Astro.props;
---
<span class:list={['inline-flex items-center gap-2 font-din text-[13px] font-bold tracking-[0.16em] text-solar uppercase', className]}>
  <slot />
</span>
